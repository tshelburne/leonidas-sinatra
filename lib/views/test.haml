!!! 5
%html
  %head
    = load_asset("commands.js", :base => "assets")

    %script
      require("patches");
      require("environment");

      environment.addUrl("sync", "http://localhost:9393/sync");

      var App = require("app");
      var app = new App(#{@command_source.id});

  %body
