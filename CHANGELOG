v0.0.9 added support to check if leonidas believes online connectivity is available

v0.0.8 added has_run? to server side, and fixed bug related to new clients opening existing apps

v0.0.7 removed state from ruby app and js client - the concept of state only exists in the handlers that affect it

v0.0.6 full client-as-authority reconciliation following a server crash or restart while app is live

v0.0.5 step one into having client-as-authority reconciliation following server crash or restart

v0.0.4 revised both frontend and backend code to use command rollbacks rather the state locking

v0.0.3 removed jquery, replaced with reqwest for simple ajax

v0.0.2 revised library to have unified terminology throughout

v0.0.1 initial library
